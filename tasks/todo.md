# Vector Unified — Task Checklist

> **Status:** Live · updated 2026-07-10 (Stage 2 + market layer reconciled)
> **Plan:** [`plan.md`](./plan.md) · **Spec:** `docs/SPEC.md` · **Arch:** `docs/UNIFIED_ARCHITECTURE.md`
> **Shipped:** Stage 1 v0.1 (`pipeline/data/unified_best.pt` + `assets/unified.json`) — do not overwrite unless G2+G1 ship criteria met.

## Phase 0 — Foundations
- [x] 0.1 Confirm 3 encoders load read-only; record shapes + 3-player fingerprint per sport
- [x] 0.2 Author `data/archetype_map.json` (12 cross-sport archetypes × 3 sports; pitch A6/A7/A9/A10 = n/a)
- [x] 0.3 Author `data/analogy_triples.json` (40 cross-sport analogy triples) + `analogy_triples_eval.py` — G4-curated arch-agreement 0.675 (target 0.60) PASS; retrieval hit-rate 0.000 reframed as wrong-metric-for-large-pools (mean B-rank 2149 vs random ~6950 = 3.23x better-than-random); showcase reveals model splits initiator(A0)/volume-scorer(A1) and grinder(A5)/connector(A11) — genuine data-driven distinctions. Low-rank wins: Gobert~VVD r11, Tyreek~Mbappé r32, Henderson~Draymond r44.
- [x] 0.4 Read sibling conventions (gridiron/hoops/pitch train scripts); match naming

> **Orient findings (2026-07-10, evidence not assumption):**
> - Hoops `e_h` cached at `vector-hoops/pipeline/data/embedding_v3.npz` (E 12966×48 + player_id/season/name/cluster/position; L2-normed). Shipped 8 native clusters + `mtnn_arch.json` labels.
> - Gridiron ships NO 32-d asset (`assets/embedding.json` is PCA-3 map only). `e_g` regenerated from `mtnn_best.pt` (wrapper {state,mu,sd,feats,families,n_seasons,season_min,d_emb}) over `train_matrix.npz` (49881 weekly rows) → season-aggregated to 5325 player-seasons. MTNN class copied minimal (no ingestion-layer import).
> - Pitch `e_p` cached at `vector-pitch/assets/pitch_mtnn_embeddings.json` ({players:[{...,e_p[24]}]}, 2430 rows).
> - Total 20,721 player-seasons; all L2-norm = 1.0 verified. `data/native_clusters.json` derived; v0 `native_to_cross` committed (A0-A5,A11 in scope; A6-A10 deferred — need feature-based labeler; gridiron has no A3/A4 — offense-only data).

## Phase 1 — Pitch MTNN prerequisite
- [x] 1.1 PitchMTNN — 16 feats → 3 families → 24-d embedding; archetype CE + profile recon (`vector-pitch/pipeline/train_mtnn.py`)
- [x] 1.2 Validate vs PCA+k-means baseline — **4/4 role-recovery** on `tm_9ctx` LOO (`pipeline/data/pitch_mtnn_report.json`); PCA(16) oracle still ahead (knn5 ~0.79 vs ~0.76)
- [x] 1.3 Export `e_p` + `pitch_mtnn.pt`; `vectors.json` untouched (additive)
- [x] 1.4 Hill-climb: position SupCon (`--con-w 0.5`) — **PROMOTED**. LOO: pos-cluster 0.797 (beats PCA16 0.746), knn5 0.789≈oracle, nn_role 0.749≈oracle, recon 0.496 still beats PCA3. Export: `pitch_mtnn.pt` + embeddings; pre-con backups retained.

## Phase 2 — Joint matrix + frozen-encoder alignment (Stage 1)
- [x] 2.1 `load_encoders.py` — emit e_h/e_g/e_p per player-season (gridiron = mean weekly e_g)
- [x] 2.2 `build_unified_matrix.py` — assemble leak-free matrix; assert shapes/coverage/no-NaN
- [x] 2.3 `train_unified.py` Stage 1 — frozen encoders; trunk(64-d)+sport token+era; heads+InfoNCE+CORAL+GRL; balanced batches; rank logged/epoch

> **Stage 1 v0 results (2026-07-10, eval_unified.py G1-G3):** `unified_best.pt` = d_emb 64, λ 0.05, sport_tok dropped, w_var/cov 1, 40ep. **G1 PASS** (z beats frozen e_s on native-cluster kNN-5: hoops 0.851→0.993, gridiron 0.981→0.983, pitch 0.998→1.000; pos 1.0 all — role preserved, games won't break). **G3 PASS** (cross-sport archetype silhouette 0.688; within-arch cross-sport cos 0.749 >> between-arch -0.098 — the folding works: shared archetypes cohere across sports). **collapse_detector PASS** (rank 13.0 ≥ 12 AND G1 AND G3 — not collapsed; the role manifold is genuinely ~13-d, the literal d_emb/2=32 floor over-alarms). **G2 sport-invariance FAIL** — sport is ~79% recoverable from z (chance 33%); a no-GRL control also scores 0.799, so **the GRL is inert (Δ≈1pp)** and dropping sport_tok changed nothing (0.786). The leak is structural to the **per-sport adapter** (a distinct Linear per sport bakes sport into z). True sport-invariance needs a **shared-adapter redesign (Stage 1.1)**: pad e_s to a common dim, one shared Linear, let GRL+CORAL+SupCon do the mixing. Honest take: the user's goal (one geometry to ask "what does a PF share with a SS") IS met — G3 proves cross-sport archetypes cohere; z just also retains a sport axis (sliceable, not harmful). G2's "sport unknowable" is a stricter fairness-style goal, deferred.
- [x] 2.4 `eval_unified.py` — G1 non-inferiority, G2 sport-invariance+rank, G3 silhouette → `unified_report.json`
- [x] 2.5 **Stage 1.1 probe (p19):** shared-adapter still blocked under frozen encoders → **Stage 2 required** for G2 progress. Soft-deferred (G2 not ship-blocking for v0.1).

## Phase 3 — Analogy face-validity
- [x] 3.1 `analogy_panel.py` — cross-sport top-k NN + role label; G4 = NN-role-coherence hit-rate
- [x] 3.2 Run panel: **G4 = 0.959 (target 0.60) PASS**. Named showcase sensible (Tom Brady QB→Draymond Green/Marcus Smart @0.97; Andre Drummond C→soccer defenders Lamine Koné/Kieran Tierney @0.89; Ty Lawson PG→Kevin De Bruyne @0.98). Per-sport: hoops 0.976, gridiron 0.974, pitch 0.837.
- [x] 3.3 **Taxonomy v1 cleanup DONE:** A4 (pitch-only, n=383, G4 0.000) remapped → pitch cluster 2 now folds into A3 (defensive anchor). A4 moved to deferred (needs gridiron defensive data + hoops steals/blocks specialist cluster to become a real shared archetype). **G4 0.959 → 0.978; pitch hit-rate 0.837 → 0.993; all 6 in-scope archetypes now >0.95.** (The earlier "A6 discrepancy" was a labeling bug — index 6 = A11 in-scope — RESOLVED.)

## Phase 4 — End-to-end fine-tune (Stage 2)
- [x] 4.1 Infra: `pipeline/load_live_encoders.py` + `pipeline/train_stage2.py` (encode-path only; two LRs; post-hoc shippability)
- [x] 4.2 Clean run (`enc_lr=1e-6`): best ep26 — **G1 PASS**, **G2=0.674** (−7pp vs Stage 1 ~0.74), **SHIPPABLE=False** (target ≤0.433). Saved `unified_stage2_best.pt` + `data/stage2_history.json`. Docs: `docs/STAGE2_PLAN.md` §8.
- [x] 4.3 **Stage 2.1-a run (2026-07-30).** Precondition was NOT actually met when checked: hoops was promoted 2026-07-25 to a 64-d encoder (concat fusion, `docs/MTNN_V5_PROMOTE_GATE.md`-adjacent) but `pipeline/load_encoders.py`'s `SPORT_DIM["hoops"]` still said 48, and `load_live_encoders.py::_hoops_bundle` didn't apply the same unconditional `injury`-family exclusion / `n_injury` durability-head arg that `train_mtnn.py`'s own `main()` uses post-PR#9 — both are real bugs, now fixed (SPORT_DIM→64; family filter + `n_injury` wired). Rebuilt `unified_matrix.npz` (N=20,719), retrained Stage 1 from scratch against the corrected hoops encoder (`unified_best.pt`, rank 12.4, **G1 PASS** — hoops role actually improved 0.825→0.941, **G3 PASS** silhouette 0.594), then ran Stage 2.1-a (enc_lr 3e-5, 60ep, GRL λ 0.10 ramp 5, revert-threshold 0.02 — **staggered unfreeze from the plan was NOT implemented in `train_stage2.py` and was skipped this run**, relying on the existing per-epoch G1 revert-check instead, which is what actually caught/would-have-caught a regression). Result: **G1 holds for all 3 sports, hoops/gridiron/pitch role scores all improved slightly (no regression at all)**, best epoch 15, **G2=0.693** — plateaued there through epoch 60 (not still falling). Per the plan's own decision tree this lands on **"G2 plateau > 0.55 → declare G2 a soft target... user decision, not auto."** **User decision (2026-07-30): ship it.** Wrote `pipeline/export_unified_stage2.py` (encodes the whole corpus through the drifted per-sport encoders from `unified_stage2_best.pt["enc_states"]` + the Stage 2.1 trunk, not the frozen Stage 1 path `export_unified.py` uses) and exported `assets/unified.json` (20,719 players, 64-d, norms=1.0, per-sport counts match meta, no NaN). The G2 soft-target caveat is baked into the shipped JSON itself (`model`, `g2_sport_acc`, `g2_target`, `g2_status` fields), not just the docs. Old Stage-1 `unified.json` backed up to `assets/unified.json.pre_stage2.1_bak` (gitignored, local only). `docs/SPEC.md` §7 and this line updated to match. No live site currently reads `unified.json` (checked `vector-hub` — the Phase 5.3/5.4 "hub" copy updates were text-only methods-page framing, not a data fetch), so this is the full ship: no separate deploy step exists yet.
- [x] 4.4 **Resync after hoops hustle-defense + system-tags + gate re-anchor (2026-07-31, systems-thinking pass).** Same staleness class as 4.3, caught proactively this time (not by trying to use it and hitting a bug): `unified_best.pt`/`unified_stage2_best.pt` last trained 2026-07-30 03:2x/03:3x, predating same-day hoops changes (hustle-tracking defense family, team system tags, checkpoint-selection smoothing fix, CQS gate re-anchor — see vector-hoops `docs/MTNN_STABILITY_2026-07-30_*.md` and `composite_score.py`'s 2026-07-31 re-anchor). Rebuilt `unified_matrix.npz` (N=20,719, unchanged), retrained Stage 1 from scratch (`unified_best.pt`, rank 12.2, **G1 PASS** — hoops role improved 0.841→0.949 (+0.109), gridiron/pitch also slightly improved, **G3 PASS** silhouette 0.615), then Stage 2.1 (same recipe as 4.3: enc_lr 3e-5, 60ep, GRL λ 0.10 ramp 5, revert-threshold 0.02). Result nearly identical in shape to 4.3's: **G1 PASS all 3 sports** (hoops role_drop -0.099, i.e. improved; gridiron -0.004; pitch -0.021), best epoch 58, **G2=0.685** — same plateau class as 4.3's 0.693, not a regression from this resync, a stable property of the recipe. Same decision tree branch as 4.3 (G2 plateau, soft target) — this is a resync of an already-user-approved ship decision, not a new one, so re-applied it rather than re-asking: re-ran `export_unified_stage2.py`, `assets/unified.json` re-exported (20,719 players, 64-d, norms=1.0, no NaN, per-sport counts match). Old asset backed up to `assets/unified.json.pre_hoops_resync_<timestamp>`. Also resynced `unified_market.pt`/`unified_cultural.pt` (both **G1/G3/collapse_detector PASS**, silhouette 0.658/0.680 — near-identical to 4b.4b's 0.659/0.680, confirming the resync didn't disturb the underlying market/cultural signal) — old checkpoints backed up to `pipeline/data/unified_{market,cultural}.pt.pre_hoops_resync_<timestamp>`.

## Phase 4c — Cultural text (Wikipedia → embeddings)  [auto-mode 2026-07-11]
- [x] 4c.1 Schema `docs/CULTURAL_TEXT_SCHEMA.md` (Wikipedia-first; Reddit deferred)
- [x] 4c.2 `acquire_wikipedia_bios.py` — priority stars 47/47 ok; full unique resume in progress (429-backed-off)
- [x] 4c.3 `embed_cultural_text.py` — MiniLM 384-d via transformers mean-pool (sentence_transformers broken in env)
- [x] 4c.4 `cultural_text_join.py` — `cultural_text_matrix.npz` 20,721×384; **454 labeled seasons (2.2%)** from 47 unique
- [x] 4c.5 `train_unified.py --cultural-text --market` → `unified_cultural.pt` (warm-start market; shipped untouched)
- [x] 4c.6 Probe mean_align_cos **0.775**; eval G1 PASS / G3 PASS / G2 0.842 (expected text leak); Reddit deferred

## Phase 4b — Market / cultural anchors (full free tier)

## Phase 4b — Market / cultural anchors (full free tier)
- [x] 4b.1 Acquire Forbes earnings, Spotrac NFL salary, awards → `data/market_cultural/`
- [x] 4b.2 Join to unified matrix (`market_cultural.json`, 20,721 rows); Transfermarkt TLS blocked (pitch MARKET_VALUE gap documented)
- [x] 4b.3 Shared `salary_head` + `award_head` via `--market`; warm-start → `unified_market.pt` (does not overwrite shipped)
- [x] 4b.4 Eval: G1/G3 PASS on market ckpt; G2 still ~0.74 deferred. Probe: salary ρ≈0.64 (gridiron), award ρ≈0.52
- [x] 4b.4b **Resync after the 2026-07-30 hoops dim fix (4.3).** `unified_market.pt`/`unified_cultural.pt` still carried `sport_dim=[48,32,24]` — same staleness class as `unified_best.pt` had, just not caught until actually tried. Their join inputs were stale too: `market_cultural.json` was built for the old 20,721-row matrix (now 20,719; re-ran `market_cultural_join.py`), and `cultural_text_matrix.npz` likewise (re-ran `cultural_text_join.py` — coverage jumped 454→1,742 labeled rows (2.2%→8.4%), the Wikipedia bio acquisition backlog from 4c.2 clearly kept resuming in the background since 2026-07-22). Retrained both (`--market`, `--market --cultural-text`) against the corrected encoder: both `sport_dim=[64,32,24]` now, **G1 PASS** (hoops +0.123/+0.126, matching Stage 1/2.1's improvement), **G3 PASS** (silhouette 0.659/0.680), collapse_detector PASS. G2 0.779 (market) / 0.825 (cultural) — higher than base, as expected (more supervised signal leaks sport identity; matches the pre-existing "G2 0.842 expected text leak" note). Old stale checkpoints backed up to `pipeline/data/unified_{market,cultural}.pt.pre_dim64_<timestamp>`. Still gitignored, still not exported to any shipped asset — 4b.5's "export market-aware unified.json" remains undone, just no longer silently broken if someone picks it up.
- [x] 4b.5a **Social reach, keyless (2026-07-30).** Apify needs a paid key; swapped for the free Wikimedia pageviews REST API (no auth). New `acquire_wikipedia_pageviews.py`: for each of the 429 players already resolved to an enwiki title (`wikipedia_bios.json`), pulls monthly pageviews 2015-07→present, aggregates to annual sums. 390/429 had any data; ~1.79B total views across all player-years. Joined into `market_cultural.json` as `reach_views`/`reach_log`/`m_reach` (exact player+year match only, same no-interpolation discipline as Forbes) — coverage: hoops 115 rows (0.9%), **gridiron 1,270 rows (23.9%)**, pitch 26 rows (1.1%). Added a new `reach_head` to `UnifiedTrunk` (mirrors salary_head/award_head exactly), wired through `load_matrix`/`gather_batch`/`unpack_aux`/`market_loss`. Retrained `unified_market.pt` + `unified_cultural.pt`: **G1 PASS** (unchanged from 4b.4b), **G3 PASS** (silhouette 0.645/0.669), collapse_detector PASS. Face-validity probe (`probe_market_heads.py`, extended) — reach Spearman ρ: hoops 0.463, gridiron 0.799, pitch 0.418, **pooled 0.837 (the strongest of the three market signals)**; top-predicted names are LeBron James, Nikola Jokić, Giannis, Luka Dončić, Dak Prescott — real signal, not noise. Old checkpoints backed up to `pipeline/data/unified_{market,cultural}.pt.pre_reach_<timestamp>`.
- [ ] 4b.5b Still open: API-Football for pitch market value (needs a key — Transfermarkt direct scrape already confirmed blocked, see 4b.2); export market-aware `unified.json` for UI (no live site reads `unified.json`'s market/cultural fields yet — same as the base embedding, this is additive infra ahead of a consumer, not a regression).

## Phase 5 — Ship + Methods
- [x] 5.1 `export_unified.py` → `assets/unified.json` (20,721 players, 64-d e + PCA-3 map, cross-sport archetypes + axes; norms=1.0, counts match, loads clean). Per-sport assets untouched (additive).
- [x] 5.2 Ablation: drop each alignment loss → Δ on G2/G3/G4. **Verdict (data/ablation_report.json):** SupCon ESSENTIAL (drop→G3 0.718→0.125, G4 0.988→0.137); CORAL INERT (0Δ); VICReg INERT at default w (0Δ on rank/G3/G4); GRL modest (~5pp sport-acc, no G3/G4 harm); task-only keeps G1 but no folding (G4 0.129). **Lean config = SupCon+task+GRL (drop CORAL+VICReg).** Each loss now earns its keep.
- [x] 5.3 Methods copy (honest: "shared role space", "best-guess", pitch role-only caveat) — hub README + index updated 2026-07-11
- [x] 5.4 Upgrade `vector-hub` "Where this is going" → shipped-with-caveats (G2 deferred; no joint daily puzzle yet)

## Phase 6 — Pitch SupCon → prod unified (2026-07-11)
- [x] 6.1 Backup shipped `unified_best.pt` + `unified.json` → `pipeline/data/bak_pre_pitch_con/`
- [x] 6.2 Rebuild `native_clusters.json`; rematch pitch `native_to_cross` via pos_dist L1 (`remap_pitch_n2c.py`); rebuild matrix
- [x] 6.3 Retrain Stage 1 from scratch (same lean hyperparams); **G1 PASS, G3 PASS, G4=0.972 PASS**, G2 sport-acc **0.694** (improved vs ~0.74, still deferred)
- [x] 6.4 Export `assets/unified.json` (20,721 × 64-d, norms=1.0)
- [x] 6.5 Deploy hub to prod — `vercel --prod` 2026-07-14 → https://dumbmodel.com (vector-mx18tg0i0… Ready); live copy shows 64-d joint embedding shipped-with-caveats

## Verify (always last)
- [x] V1 G1–G3 green; no per-sport regression; collapse_detector PASS (rank 13≥12; literal rank≥32 over-alarms on a genuine ~13-d role manifold). G2 sport-invariance FAIL (structural, deferred to Stage 2). `verify_encoders.py`: 3 frozen encoders load, norms=1.0, counts match meta. Git: hoops clean, pitch additive-only, gridiron only pre-existing build_features.py M (not from unified).
- [x] V2 G4 = 0.978 ≥ 60% (target 0.60); all 6 in-scope archetypes >0.95; named panel sensible (Brady→MacAllister/Draymond @0.97; Drummond→soccer CBs @0.86; Lawson→De Bruyne @0.98).
- [x] V3 `unified.json` loads (20721 players, 64-d, PCA-3 map); per-sport assets untouched (additive); norms=1.0.
- [x] V4 Re-read diff of the four docs for internal consistency — UNIFIED_ARCHITECTURE.md §11 addendum (Stage 1 v0 results + 6 named deviations), SPEC.md §7 acceptance status annotated (G2 deferred), pitch-row + ask-first/non-goals updated to reflect Phase 1 completion + market/cultural deferral.
- [x] V5 Pitch SupCon hill-climb: LOO 4/4 vs PCA3; beats PCA16 on pos-cluster; `pitch_mtnn.pt` promoted with pre-con backup

## Phase 7 — Company layer + the trajectory axis (2026-08-02/03)

- [x] 7.1 **Company edge exists on the ORG side.** The census measured P859 on ATHLETES (0.22%) and was read as "no company edge". Never measured on orgs. `probe_company_edges.py`: sponsor 14.4%, owner 40.1%, venue named-after 35.8% — complementary (US venues carry naming rights where European clubs carry shirt sponsors). Union 142/187 orgs = 75.9%.
- [x] 7.2 **CORRECTION — 84.3% was 71.5%.** The edge probe typed targets as `organization`, which admits families (Maloof, Rooney), football federations, geographic entities and one club as its own company: 68 of 196, carrying 797 athletes. Re-typed against business/company: 128 companies, 4,451 athletes = **71.5%** (gridiron 82.6%, hoops 95.7%, pitch 24.7% — pitch fell from 63.4%). `is_business` is emitted per company, not used as a silent filter, because typing has false negatives too (GEHA).
- [x] 7.3 **Sector taxonomy, 28 sectors, 127/128 = 99.2%** (`data/sector_map.json`, `apply_sector_map.py`). Hand-authored ONLY after both automatic routes were measured and rejected: P279* rollup 51/128 = 39.8% (misses "airline", "software company", "cryptocurrency exchange"); NAICS/ISIC/SIC/GICS **0/128**. Insurance and Crypto kept distinct from Financial Services per operator. Brand-exposure figure excl. `sports_holdings`: **4,425 = 71.07%**.
- [x] 7.4 **Q8/Q9 — skill→sponsor is TESTED and NULL.** Q8 could not run: "has a naming-rights deal" is 96% true in US sport. Q9 with sector: hoops L1 0.049 vs p95 0.067, gridiron 0.044 vs 0.056, pitch 0.225 vs 0.241 — three nulls, positive control passing at r=+0.968. **The ceiling is league structure**: 30 NBA + 32 NFL franchises means ~31 orgs per US sport, and `team × season` does not buy independence because sector is not time-sliced. More company acquisition does not fix this.
- [x] 7.5 **Role/trajectory split.** `archetype_map.json` now carries `axes.role` (grain player-season, A0–A5/A10/A11) and `axes.trajectory` (grain career, T0–T3, was A6–A9) plus `legacy_a_to_axis`. Blast radius measured and much smaller than first stated: A4/A6/A7/A8/A9/A10 are declared but **never assigned**, and the map is read only inside vector-unified (14 files) — hoops/gridiron/pitch/equities/dottie reference it zero times.
- [x] 7.6 **T0/T1 ASSIGNED for hoops** (`build_trajectory_axis.py` → `data/trajectory_axis.json`). expectation = `pedigree.json expect_slot`; delivery = career-mean `impact` percentile (contract-blind, era-neutral). 1,308 careers, corr(expectation, delivery) **+0.26** — draft slot explains ~7% of delivery variance. T0 117, T1 100, 1,091 unlabelled by design. Face validity: T0 led by Anthony Bennett (#1), Jahlil Okafor (#3); T1 by Jokić (41), Ginóbili (57), VanVleet (undrafted).
- [x] 7.7 **Gridiron pedigree exported + T0/T1 assigned.** `export_gridiron_pedigree.py` -> `data/gridiron_pedigree.json` (2,704 athletes, 1,901 = 70.3% with a draft row, 803 undrafted; 400 name collisions resolved by keeping the earlier pick). `build_trajectory_axis.py --sport gridiron`: 1,192 careers, corr **+0.4236** vs hoops' +0.2598. T0 77, T1 86. Face validity: T0 Desmond Howard (4), David Carr (1), Trubisky (2); T1 Arian Foster (undrafted), Shannon Sharpe (192), Tyreek Hill (165), Kittle (146). Delivery is PPR percentile WITHIN (season, position) — unnormalised would rank every QB above every TE. Scope: QB/RB/WR/TE only; all 18 gridiron features are pass/rush/receiving.
- [x] 7.7b **Tested, and the difference is REAL.** `compare_trajectory_sports.py`, 10,000 bootstrap resamples, seed 7: hoops r=+0.2598 CI [0.2034, 0.3131]; gridiron r=+0.4236 CI [0.3767, 0.4691]; difference **+0.1638 CI [+0.0927, +0.2341], excludes zero**. Position stratification KILLS the leading alternative explanation and inverts it — RB +0.5709, TE +0.4760, WR +0.4714, **QB +0.1224 (below hoops)**. A composition artefact would show one dominant position carrying the pooled number; instead the pooled number is dragged DOWN by its smallest group. Remaining binding caveat: delivery constructs differ (`impact` composite vs fantasy PPR).
- [x] 7.7c **QB: no detectable relationship, and restricted range is REFUTED.** Per-position bootstrap CIs: RB +0.5709 [+0.510,+0.630], TE +0.4760 [+0.386,+0.561], WR +0.4714 [+0.398,+0.541], **QB +0.1224 [-0.053,+0.288] — includes zero**, and does not overlap RB. The obvious mechanical explanation was restricted range; it is refuted and INVERTED — QB has the WIDEST draft-expectation spread of any position (sd 0.313 / IQR 0.601 vs ~0.15-0.20 / 0.23-0.30). Attenuation would need the opposite. **Remaining alternative, NOT excluded: survivorship.** A high-pick QB who busts is benched before four charted seasons, so the QB pool is the most heavily selected on delivery (undrafted share 10.3% vs 20-25% elsewhere). Testing it needs the players who left, which this dataset does not contain.
- [x] 7.7d **Survivorship CONFIRMED — and it corrects 7.7c's framing.** `probe_qb_survivorship.py` joins `draft_picks.csv` (the DENOMINATOR: every pick) to the survivor-only vector set, draft years 1999-2022 so everyone had 4 seasons of opportunity. QB survival **28.5%** (lowest of the four) with slot->survival corr **+0.5973** (highest). Bucket gradient is brutal: QB R1 70.8% -> R4-7 **9.0%**, against RB 84.6% -> 26.7%. **So draft slot at QB buys SURVIVAL, not production-among-survivors.** 7.7c's "where they were taken tells you nothing" was wrong — it tells you a great deal about whether they last, and little about output conditional on lasting. Directional only: no CI on the correlation difference.
- [x] 7.7e **Draft-value curve built** (`build_draft_value_curve.py` -> `data/draft_value_curve.json`). EV = P(survive|pos,bucket) x E[delivery|survive]. **11 of 15 cells change rank once bust risk is priced in.** Conditional order RB R1 > TE R1 > WR R1; expected-value order **TE R1 > RB R1 > WR R1** (TE R1 survives 96.0%). The QB truncation is now visible in one line: **QB R4-7 conditional delivery 54.5 — higher than QB R2's 40.7 — but survival 9.0%, so EV 4.9, the worst cell on the board.** Late-round QBs who survive are good; almost none survive. Found and fixed a pool mismatch mid-build: survival was windowed to 1999-2022, delivery was not (1980-2025), which produced TE R1 with 25 drafted and 28 survivors scored. 144 of 951 delivery careers were out of window.
- [x] 7.7f **VOR draft-value table — cross-position comparable, after three corrections.** `build_vor_draft_value.py`. 12-team 1QB/2RB/3WR/1TE (declared; superflex moves QB). Final: **RB R1 12.69 > WR R1 7.42 > RB R2 6.49 > TE R1 3.94 > RB R3 3.80 > WR R2 3.45 > QB R1 3.20**, monotone by round within every position. Matches fantasy convention — RB1 scarcity on top, QB1 cheap in a 1QB league. THREE ERRORS FOUND AND FIXED IN SEQUENCE: (1) `P(survive) x E[VOR|survive]` is invalid for signed values — 78.2% of charted seasons are below replacement, so lower survival made picks look BETTER (QB R4-7 ranked 2nd at -0.30); (2) `gridiron_pedigree.json` is NOT the denominator, it was built FROM the vector set, so iterating it reproduced survivor bias (`never played` read 0.0% where the truth is 71.6% for QB R4-7) — must read `draft_picks.csv`; (3) VOR must FLOOR AT ZERO per season, because a manager benches a below-replacement player rather than starting him — unfloored, a pick that produced nothing outranked a franchise QB.
- [x] 7.7f-OLD **EV is not cross-position comparable.** Delivery is a percentile WITHIN (season, position), so "TE R1 = 70.4" and "QB R1 = 40.2" are scored against different pools. Making them comparable needs positional scarcity (one QB starts per team) and a common scoring scale — neither is in this number.
- [x] 7.7e-OLD **The composite QB claim is now testable and unmade.** P(survive | slot) x E[delivery | survive, slot] is the quantity a draft-value model actually wants, and both halves are now measured. Nobody has multiplied them.
- [x] 7.7d-OLD **Survivorship probe for QB.** Would need draft classes joined to whether a player ever reached 4 fantasy-relevant seasons — i.e. the DENOMINATOR, not just survivors. `draft_picks.csv` has every pick; the vector set has only survivors. The join exists to test it: attrition rate by draft slot at QB vs RB.
- [x] 7.7c-OLD **QB is the finding worth chasing.** Draft slot barely predicts fantasy delivery at quarterback (+0.12, n=116) while predicting RB delivery at +0.57. That matches football-side belief that QB is the hardest position to draft, and it is the first cross-sport-model result that says something a domain expert would recognise and did not already get from the model.
- [x] 7.7b-OLD **The two correlations are not strictly comparable** — hoops delivery is `impact`, gridiron is fantasy PPR. Same scoring rule, different constructs. A real cross-sport claim needs one delivery definition or an explicit argument that both measure the same thing.
- [x] 7.7-OLD **Export gridiron pedigree as a joinable artifact.** It has the inputs (`draft_pick_log`, `draft_round`, combine) plus consensus ADP — `build_adp.py`'s docstring literally describes flagging "where the model likes a player more than the room is paying for him" — but nothing is exported name-joinable. This is the one step that makes T0/T1 cross-sport.
- [x] 7.8 **T2/T3 resolved by SPLITTING the axis — D0/D1 built for both sports** (`build_direction_axis.py`). Reading T0-T3's own definitions shows two questions under one label: T0/T1 is standing vs EXPECTATION (career-level, vs draft slot); T2/T3 is DIRECTION over TIME (within-career, early vs late). A player can be both at once — Jokic at 34 is T1 and T3 — so one mutually-exclusive axis makes that inexpressible. Same category error caught twice already this session (role vs trajectory `590cd0d`; role-change-pattern vs performance-vs-expectation `b75f11a`); third time it was applied rather than argued. New prefix D rather than reusing T2/T3 so a stale consumer gets a KeyError instead of silently reading a direction as a standing. Method: per-season floored VOR split into halves, delta = mean(second) - mean(first), tails only at 20%, MIN_SEASONS 4. **gridiron** 460 careers, D0 92 / D1 92 (D0 Antonio Brown pick 195 +6.93, Calvin Johnson; D1 Odell Beckham -9.01, Ezekiel Elliott, Le'Veon Bell). **hoops** 568 careers, D0 114 / D1 114 (D0 Giannis pick 15 +16.93, Jokic pick 41 +12.65, Haliburton, Barnes, Tatum; D1 Vince Carter -12.24, Elton Brand, Kirilenko). Floored VOR means a career entirely below replacement has delta 0 and is DROPPED, not labelled — "bad then bad" is not a direction; D0/D1 therefore describe a narrower population than "all careers" and say so in the artifact.
- [x] 7.8a **Two copies of one rule — the eligibility bypass, and the invariant that now catches it.** `build_direction_axis.py` carried its OWN composite/replacement loop, so when the minutes gate was added to `build_hoops_vor_draft_value.py` the axis kept reading the raw per-100 cache and reported **Damion James, pick 24, 0.00 -> 38.16, the biggest riser in the NBA, on ONE eligible season**. `base_<season>.json` is raw; `vectors.json` applies a schedule-aware gate (572 raw -> 484 eligible in 2023-24) and the composite is per-100 possessions, so a few minutes posts an enormous rate. Fixed structurally: `eligible_pairs()` + `vor_series()` are now THE ONE implementation in the value-table builder, imported by the axis. **I6 added** to `check_draft_value_invariants.py`: the axis and the value table must agree on qualifying-season count per career (both now emit `seasons_total`). NON-VACUITY: re-injecting the exact historical bypass fires I6 on **205 careers** and brings Damion James back; restored clean. Nothing in I1-I5 could see it — both artifacts were internally consistent and individually plausible, and merely described different populations.
- [x] 7.10 **The 71.5% company reach is not a sponsorship number, and none of the variance is athlete-level.** `probe_athlete_sector_signal.py`. Two findings, both structural, both correcting claims already written down. (a) **RELATION SPLIT:** sponsor 870 athletes **13.97%**, owner 750 12.05%, named_after 4,055 **65.13%**, union 4,451 71.49%. Sixty-five of the 71.5 points are STADIUM NAMING RIGHTS — whose name is on the building, not a sponsorship of anyone. Also two disjoint markets summed: named_after is US (hoops 94.5%, gridiron 82.6%, pitch 5.0%), sponsor is the European shirt pattern — and sponsor lands at 10-16% in EVERY sport, so ~85% of athletes have no team-level sponsor edge at all. The census had already been corrected once (84.3% -> 71.5%) and its "the sponsorship-relevant figure is 71.5%" line was wrong the same way, one level down; struck out in `VALUE_SIGNAL_CENSUS.md` and `TAXONOMY.md`. (b) **VARIANCE IS ENTIRELY TEAM-LEVEL.** The layer is transitive by construction (athlete -> org -> company, because athlete-side P859 is 0.22%), so an athlete's sector vector is a deterministic function of his team set and teammates are byte-identical: 4,451 athletes -> **864 distinct sector vectors (5.2x collapse)** over 2,231 team sets, largest class **480 athletes all `financial_services`**. Archetype, T0/T1, D0/D1, Forbes rank and honors contribute **exactly zero** to predicting sector — and an athlete-level train/test split would still score well, because it puts teammates on both sides. Honest split has **2,231 independent units, not 4,451**. Leaves standing: team <-> sector matching. Does not leave standing: athlete <-> sponsor fit, which has no labels here to fit or validate against. Fourth appearance of the denominator problem this phase.
- [x] 7.11 **Company edges are not purely a Wikidata-coverage artifact — but only one sport can answer, and the answer is weaker than the label.** `probe_company_edge_confound.py`. Before describing the layer as commercial, tested the boring alternative: a team written about more has a fuller Wikidata item and therefore more P859/P127/P115 statements. Predictors log10 venue capacity (physical, cannot be inflated by coverage) vs log10 Wikidata sitelinks (notability); outcome BINARY has-any-business-sector-edge, forced by the density — **135 of 222 teams have 0 sectors, 71 have exactly 1, only 3 have >2**, so a per-team sector profile does not exist and a "whitespace" lead list would answer "26 of 28 missing" for the modal team. 161 teams usable (dropped 35 no QID, 26 no capacity). Result: **pitch n=101, r_cap +0.2212, r_site +0.3154, partial r_cap|site +0.1742 CI [+0.0528, +0.3341] excludes zero.** Capacity survives controlling for notability — but notability is still the STRONGER single predictor, so the honest reading is "commercial scale adds something beyond how much has been written", not "the layer measures commerce". **hoops and gridiron cannot answer at all: 28/30 and 23/30 teams have an edge, so there is nothing to explain** — in US leagues a company edge is near-universal and its presence carries no information. **THE PRE-REGISTERED RULE PRODUCED A FALSE POSITIVE AND IT IS LEFT IN THE FILE.** It tested significance but never minority-class count: hoops returned COMMERCIAL SIGNAL on a CI excluding zero while carrying **exactly 2 teams without an edge**, with a NEGATIVE sign (bigger arena -> less likely to have an edge). Amendment `MIN_MINORITY = 15` disclosed in the docstring with the first-run numbers rather than retro-fitted; the original verdict still prints for every group, marked NOT DECIDING.
- [x] 7.12 **Athlete-level market layer is a STAR-ONLY sample, has no football in it, and the name index was dating 20th-century seasons a century late.** `probe_market_layer_coverage.py` + a real bug fix in `resolve_names.py`. (a) **COVERAGE.** `honors_wikidata.json` 8,073 rows -> 525 corpus athletes; `social_handles.json` 12,968 rows -> 1,299; `award_prestige.json` 512 matched (hoops 333 = 13.8%, pitch 175 = 9.5%, **gridiron 4 = 0.3%**); Forbes 160 rows -> 24 corpus athletes. Unmatched rows are mostly SCOPE not dirt — WNBA (Anne Donovan, Cynthia Cooper-Dyke), European leagues (Maik Zirbes) — but genuine non-persons are in there too (ARCO Arena, Roseto Sharks), so a row count is not an athlete count. (b) **GRIDIRON HAS NO ATHLETE-LEVEL MARKET DATA AT ALL** — both raw pulls returned zero gridiron rows. Any cross-sport marketability claim made today contains no football. Query-scope gap in `pull_honors_wikidata.py`, fixable. (c) **SELECTION TEST, pre-registered before the run** (STAR-ONLY if top-decile coverage >= 5x bottom): hoops career-VOR deciles -> award_prestige coverage runs **7.1, 3.3, 10.8, 5.8, 10.0, 7.9, 12.4, 16.1, 22.0, 42.6%** — **ratio 6.0, STAR-ONLY SAMPLE.** Any "marketability vs production residual" computed on this pool describes stars, not athletes. Fifth appearance of the selection problem this phase. Also visible: **6 of 10 deciles have median career VOR 0.00** — most eligible hoops careers never clear replacement. Forbes additionally unusable as a continuous outcome: absence from a 10-per-year list is not zero earnings. (d) **BUG FIXED — `season_year()` dated every 20th-century season a century late.** `end_i + 2000` turned "1996-97" into **2097**; AC Green's career read `[2000, 2001, 2097, 2098]`. **1,168 of 20,689 values (5.6%) across 530 of 5,837 players.** Century now comes from `start`. Nothing consumed `season_years` yet so nothing downstream was wrong — but the field's own docstring says it exists "for season-varying joins", exactly the use that would have attached 1996 data to 2097. Guard `_verify_season_years()` refuses to write if any derived year is more than a year from its own label; mutation-verified (re-inject `+2000` -> exit 1, restored -> exit 0). Skips pitch's "Euro 2020"/"WC 2018" labels rather than guessing.
- [x] 7.13 **The gridiron sport QID was an Italian town, and fixing it exposed that "matched" was never the coverage number.** `SPORT_Q["gridiron"]` was **Q9398 = Grugliasco, an Italian comune**, not Q41323 (American football) — so every gridiron Wikidata query asked for people whose sport is a town near Turin and got back an empty result set, which is indistinguishable from "no matches" and never errored. 7.12 measured the hole from the far end before the cause was found. After the fix: honors 0 -> 1,111 rows, handles 0 -> 4,510 rows, award_prestige 4 -> 57 players, **gridiron social handles now 648/1,572 = 41.2% of corpus, the best-covered sport**. Guard added: `pull_honors_wikidata.py` refuses to write if any sport produced zero honors or zero handles rows; mutation-verified (restore Q9398 -> exit 1). **AND THE REAL NUMBER IS MUCH SMALLER THAN "MATCHED".** 399 of 568 matched rows (**70.2%**) score AWARD_PRESTIGE **0.0** — a row exists because the athlete has some Wikidata award, but untiered awards score nothing. Usable coverage is hoops **131 = 5.4%** (not 13.8%), pitch **31 = 1.7%** (not 9.7%), gridiron **7 = 0.4%** (not 3.6%) — **169 of 5,820 athletes = 2.9% carry a nonzero athlete-level market value.** Of those, **16 sit at the 1.000 ceiling spanning 5 to 675 raw awards** (Jokic ties Messi), so the measure cannot rank the top of its own range, which is the only range a marketability product would use. Also killed a stale hard-coded narrative: `probe_market_layer_coverage.py` was asserting "GRIDIRON HAS NO ATHLETE-LEVEL MARKET DATA" next to live numbers that contradicted it — the note is now computed from the measurement.
- [x] 7.14 **The bad QID was in a SECOND file, and the 0.22% census it produced turns out to be right anyway.** Swept for the 7.13 defect class and found `acquire_sponsors.py:60` carrying the same `"gridiron": "Q9398"` — the wrong value had been copy-pasted between files. That script produced the **0.22% athlete-side P859 census** quoted all through Phase 7 as "athlete-level sponsorship does not exist in Wikidata", and it reported gridiron `with_sponsor: 0`, which looked like the QID rather than a measurement. **Re-ran with Q41323 and the headline did not move:** edge athletes 43 -> 44, matched 13 -> 13, **matched_pct 0.22 -> 0.22**, gridiron still 0/1,573. Exactly **one** gridiron athlete in all of Wikidata carries P859 — Greg Scruggs -> Boys Hope Girls Hope, a charity — and he is not in the corpus. So the load-bearing claim stands, and it is now measured with football actually queried instead of structurally absent. Expected a correction, got a confirmation; recording that rather than quietly moving on. **`check_wikidata_qids.py` added** so the class cannot recur: scans `pipeline/*.py` for operative QID literals (`wd:Q…`, `entity/Q…`, or a quoted `"Q…"` — a bare `\bQ\d+\b` matched "Q1."/"Q2." docstring numbering and produced 8 false positives on the first run), resolves each against live Wikidata, and fails on **mismatch, missing, UNREGISTERED, or BANNED**. BANNED is separate from EXPECT on purpose: a registry that only checks identity would happily pass a revived Q9398 — *"Q9398 is Grugliasco, correct!"* — which answers the wrong question. 12 operative QIDs, 12 registered, 1 banned. Mutation-verified on both paths: revive Q9398 -> exit 1 (UNREGISTERED + BANNED), add an undeclared Q12100 -> exit 1; restored -> exit 0.
- [x] 7.15 **One gate, plus a self-review that found a live collision.** `pipeline/validate.py` runs every checker together — Phase 7 built several and **nothing ran them as a set**, so each was mutation-verified once and then left to be remembered. A checker nobody invokes is a comment with a shebang. Registration is mandatory and mirrors `check_wikidata_qids.py`'s own rule: `check_*.py` is discovered by glob and an **unregistered checker is a FAILURE, not a skip** (mutation-verified — drop in an unregistered `check_bogus_tmp.py` -> exit 1). Network is declared per check, `--offline` SKIPs rather than reporting an unearned pass. Guards embedded in builders (`resolve_names.py::_verify_season_years`, `pull_honors_wikidata.py` empty-sport refusal) are **listed as not-exercised** rather than counted, because re-running them costs a live pull and claiming them would be the exact unearned green this file exists to end. Current: `2 PASS, 0 FAIL`. **Self-review of the session diff found a real defect:** `probe_market_layer_coverage.py` keyed AWARD_PRESTIGE by bare normalised name, so gridiron's Chris Johnson marked hoops' Chris Johnson as covered — one collision, but the same unscoped cross-sport join that produced 17/17 false matches earlier in this phase (NFL Matt Ryan -> NBA Matt Ryan). Now keyed `(sport, name)`; selection-test ratio **6.9 -> 7.3**, verdict unchanged (STAR-ONLY). Also removed a stale athlete count from a docstring — the file had already had that bug once.
- [x] 7.16 **Mutation-tested the MODEL gates. Two thresholds were wrong; both findings survive.** `check_gate_nonvacuity.py` re-scores G1/G2/G3 on three nulls — `global_shuffle` (permute z everywhere), `within_sport_shuffle` (permute z INSIDE each sport, preserving sport geometry and destroying only the player->archetype link — the sharp one), `random_gaussian`. Pre-registered before the run: a gate passing on `within_sport_shuffle` is not measuring what it claims.

        variant                   G1   G2acc   rank      sil   within  between      sep
        REAL                    True   0.755   12.4   0.6147   0.7030  -0.1418  +0.8448
        global_shuffle         False   0.626   12.4  -0.0161   0.0143  -0.0023  +0.0167
        within_sport_shuffle   False   0.752   12.4  -0.0307  -0.0038  -0.0066  +0.0027
        random_gaussian        False   0.626   64.0  -0.0054   0.0010   0.0025  -0.0015

    **G1 and G3-silhouette are earned** — both fail all three nulls, silhouette going negative. **Two threshold defects found.** (a) **G2's baseline was wrong**: it quoted `chance = 1/3`, but the sports are **12,966 / 5,323 / 2,430**, so always answering "hoops" scores **0.6258** — and `global_shuffle`, which carries no sport information at all, scored **0.6257**, confirming it. Sport leakage is **+0.130 over majority, not +0.422 over uniform**; the long-standing "sport is ~79% recoverable, chance 33%" framing overstated it by 29 points. `delta_vs_majority` added; `chance` kept so old reports stay readable. (b) **G3's separation test was `within > between`** — a bare inequality on a noisy difference, so it passes on a null about half the time by construction, and it did: null separations up to **+0.0440** across 50 within-sport shuffles (p95 **+0.0301**). The real +0.8448 clears that by ~28x, so **the construct and the finding were never in doubt — the TEST was**. Replaced with calibrated `SEP_FLOOR = 0.05`, provenance recorded in `data/gate_nonvacuity.json`. Also measured G3's sport-pair confound: within-arch pairs draw 51.3/23.4/25.3 across (gridiron+hoops, gridiron+pitch, hoops+pitch), between-arch 55.3/14.5/30.2 — **max gap 8.9 pct points**, present but small relative to the effect. Registered in `validate.py` (86s); gate now `3 PASS`. Stale prose in `eval_unified.py`'s docstring corrected rather than left beside the new numbers.
- [x] 7.17 **G4 null-tested too — and it is the cleanest gate in the repo.** Added G4 (cross-sport NN shares archetype) to `check_gate_nonvacuity.py` with a **computed** random baseline, because its 0.60 bar was stated without one and G2's `chance = 1/3` had just been shown to be 29 points too generous. The honest null is P(a random OTHER-SPORT row shares the query archetype) given the actual mix (0.249 / 0.235 / 0.192 / 0.148 / 0.113 / 0.063) = **0.1712**.

        variant                  G4
        REAL                 0.9401
        global_shuffle       0.1679
        within_sport_shuffle 0.1671
        random_gaussian      0.1712

    **All three nulls land on the computed baseline**, which validates the baseline and the nulls against each other — neither was assumed. Real lift **+0.7689**, so the 0.60 bar is a real bar and G4 needed no correction: hoops 0.933, gridiron 0.959, pitch 0.935 on `unified_best.pt`. `analogy_panel.py` now emits `random_baseline` and `lift_over_random` alongside the hit rate so the number is never quoted bare again. Ruff-clean; four pre-existing unused imports removed across `eval_unified.py` and `analogy_panel.py`. Gate still `3 PASS`.
- [x] 7.18 **The "3.23x better than random" retrieval salvage was an artifact of the wrong baseline. Corrected: 0.98x — indistinguishable from random.** `analogy_triples_eval.py` reported top-10 hit-rate **0.000** and reframed it as a wrong-metric problem, offering `mean B rank 2149 vs random ~6950 = 3.23x better-than-random` as the salvage. **The baseline was `avg_cross_pool / 2.0` — the expected rank of ONE randomly placed row. But `b_best_rank` is the MINIMUM over every row B has**, and a player with k charted seasons gets k draws at the target. E[min of k uniform draws from N] = **(N-k)/(k+1)**, not N/2, so the old baseline overstated the ratio by roughly the number of B rows. Corrected: mean B rank **2114.2** vs random **2067 analytic / 2048 simulated** (both computed and required to agree — a formula and a simulation disagreeing means one is wrong with no way to tell which) = **0.98x, slightly WORSE than random.** The 0.000 was never a metric problem being reframed; **the curated-pair retrieval carries no signal**, and `metric_note` now says so instead of the old salvage. What survives: **arch-agreement 0.650 vs its own computed random baseline 0.162 = lift +0.488** (baseline added for the same reason — a rate against a bar with no null is uninterpretable), intuition-matches-model-arch **0.825**, and the automated all-players G4 **0.9401 vs 0.1712** from 7.17. Third baseline defect in three tasks — G2's `chance = 1/3`, G4's unstated bar, and now this — all the same shape: **a baseline asserted rather than derived from how the statistic is actually computed.**
- [x] 7.19 **Swept the remaining thresholds. One quantity turns out to be UNTESTABLE by this method, and says so now instead of reporting a green.** Grepped every `*_pass` / `target` / threshold in `pipeline/` after three consecutive baseline defects. Two left. (a) **`silhouette_pass = sil > 0`** — a bare inequality with no margin. It was *not* actually broken: the nulls land at **-0.0161 / -0.0307 / -0.0054**, so `> 0` did separate real (+0.6147) from null. But a test with no margin is untrustworthy whether or not it has failed yet, so `SIL_FLOOR = 0.05` added, above the observed null maximum, with the same provenance treatment as `SEP_FLOOR`. Verified: real passes, all three nulls fail. (b) **`rank_nondeg_pass` cannot be tested by any permutation null, and that is a property of the statistic, not a gap in the harness.** Effective rank is a function of the singular values and a row permutation leaves the Gram spectrum untouched — **both shuffles score 12.4, exactly the real value**. `random_gaussian` moves it the wrong way, **UP to 64.0**, so a high rank is not evidence of quality either. The checker now prints this exclusion under the green line rather than letting `rank_nondeg_pass = True` be read as earned, and `eval_unified.py` carries the same warning at the definition: it detects **collapse only** and must never be quoted as evidence that z carries role structure. Note the margin is thin regardless — real 12.4 against a floor of 12.
- [x] 7.20 **Stage 2's shippability bar was UNREACHABLE, and three more defects in the same file. `shippable` flips False -> True. NOT PROMOTED — that call is the operator's.** Carried the 7.16 baseline correction into Stage 2 and found four things. (a) **`g2_pass = acc <= 1/3 + 0.10` = 0.4333 was not merely wrong, it was IMPOSSIBLE.** A majority predictor scores **0.6258**, and a perfectly sport-invariant z gives a classifier nothing but the class prior — so 0.6258 is the FLOOR of achievable accuracy and scoring 0.4333 would require z to actively mislead. Stage 2 has been reported **SHIPPABLE=False since Phase 4 against a bar no embedding could clear.** Corrected target = majority + 0.10 = **0.7258**; Stage 2 scores **0.6856 -> PASS**, G1 z beats e_s in all three sports, G3 silhouette 0.7339, **`shippable=True`**. **BUT THE BAR IS WEAK AND SHOULD BE READ THAT WAY:** delta_vs_majority is **+0.0598**, i.e. sport is still partly recoverable from z. Stage 2 roughly halves Stage 1's +0.1295 leakage; it does not achieve invariance. Fixed in both `stage2_eval.py` and `train_stage2.py`. (b) **`intuition_a_match` was structurally 0.0000 in every Stage 2 report ever written** — `t["role_intuitive"]` is a string label `"A0"`, `a_arch` is an int index, and `"A0" == 0` is always False. A hard zero that reads as "the model never matches human intuition" when it means "these were never comparable". Fixed via `ARCH_NAMES`; now **0.825, exactly matching `analogy_triples_eval.py`** — two independent implementations agreeing is the confirmation. (c) **`random_rank = (n-1)/2.0` had two errors in one line**: full pool instead of the cross-sport pool, and B treated as a single row when `b_best_rank` is the minimum over all k of B's rows. Same defect as 7.18. (d) **The same field name meant opposite things in two files** — `better_than_random_ratio` was `mean/random` here and `random/mean` in `analogy_triples_eval.py`, so the two reports disagreed on whether higher is better. Now higher-is-better in both, and both report **0.978x** on **mean_b_rank 2114.2** — identical, another cross-implementation check.
- [ ] **OPERATOR DECISION — promote Stage 2?** `shippable=True` now that the impossible bar is fixed. Nothing has been promoted: `assets/unified.json` and `pipeline/data/unified_best.pt` are untouched Stage 1 v0.1. Promoting is the operator's call, and the honest summary is: Stage 2 improves G1 role recovery in all three sports and cuts sport leakage from +0.1295 to +0.0598 over the majority floor, but is NOT sport-invariant and the gate it now passes only asks for "within 10 points of the achievable floor".
- [x] 7.21 **`pos_knn5 = 1.0` in every report since Phase 2 was a MASK USED AS AN INDEX. It scored a perfect 1.0 on a globally shuffled embedding.** Started by sweeping the sibling repos for the asserted-baseline defect and found none — vector-hoops/gridiron/pitch score against PCA+k-means comparators, not against constants, which is the right design and immune to it. **Expected to find it and did not; recording that.** Then noticed what the sweep made visible: `pos_knn5_e_s` and `pos_knn5_z` were **exactly 1.0** for all three sports, both arms, in every report ever written. A metric that returns a perfect score for everything measures nothing. Null test: **`pos_knn5_z = 1.0` on `global_shuffle` too.** Root cause: `pos_mask` is `int64 {0,1}` (`build_unified_matrix.py:115` writes `np.int64`), so `emb[mask]` in `knn5_acc` was **integer fancy-indexing, not boolean masking** — it selected row 0 or row 1, 12,966 times. `unique rows = 2 of 12966`; labels collapsed to `{1: 12925, 3: 41}`; kNN then separated two distinct vectors with two distinct labels, perfectly, always. Fixed with `np.asarray(mask).astype(bool)` plus a length assertion that makes a recurrence loud. **True numbers, with majority baselines added (an accuracy with no null is not interpretable):**

        Stage 1        native e_s / z  [base]        pos e_s / z  [base]
        hoops          0.841 / 0.949   [0.170]       0.738 / 0.784  [0.211]
        gridiron       0.977 / 0.987   [0.175]       0.999 / 0.999  [0.397]
        pitch          0.961 / 0.986   [0.189]       0.893 / 0.879  [0.437]

    Nulls now collapse to majority (hoops 0.2104 vs base 0.2107) — the metric is live. **Pitch position is the one place z is WORSE than e_s: 0.879 vs 0.893, −1.4pp**, which the old 1.0-vs-1.0 hid entirely; G1's rule is `z >= e_s - 0.02` so it still passes, barely. Gridiron 0.999 is near-saturated because its 18 features are pass/rush/receiving and the four positions have disjoint stat profiles — **a property of the data, not a model achievement**, now noted in the code. `stage2_eval.py` imports the same helper so Stage 2 was equally affected: pos was 1.0/1.0/1.0, truly **0.764/0.791, 0.999/0.999, 0.905/0.891**; `shippable` stays True (`delta_vs_majority` 0.0578). `train_stage2.py`'s `pos_drop` guard was inert for the same reason and reported `pos_ok: true` on a 0.0 drop between two 1.0s — its `verdict_from_train` block in the current report is stale from the pre-fix training run and will only be right after a retrain.
- [x] 7.22 **Four copies of G4, three of G2/G3 — consolidated to one each, and the ablation table's G4 was never comparable to the shipped one.** Swept for the 7.21 mask-as-index class first: `pos_mask` is the only non-bool mask in the estate, every other consumer uses `posm == 1` or `.any()` (both correct for int), and no bitwise `~`/`&` is applied to it anywhere. **Left `build_unified_matrix.py`'s `np.int64` alone deliberately** — int is the right dtype for a loss multiplier, and the consumer-side coercion plus length assertion in `knn5_acc` is where the guarantee belongs; rebuilding the matrix to change a dtype would add risk for no gain. Then fixed the duplication the sweep exposed. `ablation.py` re-implemented **G2** (own LogisticRegression), **G3** (own silhouette), and **G4** (own cross-sport-NN loop, **sampled to 4,000 rows for speed — so the ablation table's G4 column was never comparable to the shipped G4 sitting beside it**), and all three predated the 7.16-7.17 baseline corrections, meaning an "each loss earns its keep" decision could rest on a definition the ship gate no longer used. `g4_hit_rate()` and `g4_random_baseline()` now live in `eval_unified.py` as THE ONE IMPLEMENTATION, imported by `ablation.py`, `check_gate_nonvacuity.py` and `analogy_panel.py` (which had a fourth copy of the baseline loop). Verified by three-way agreement on the shipped z — `G2 0.7553`, `G3 0.6147`, `G4 0.940`, `G4_baseline 0.1712`, identical across all three files. `ablation.py` now also reports `G2_delta_vs_majority` and `G4_baseline` so its columns are interpretable. Seven now-unused imports removed. Gate `3 PASS`.
- [x] 7.23 **No report may be older than the code that produced it — and one was 569 hours behind.** Phase 7 redefined nearly every gate (G2's baseline, G3's two thresholds, G4's baseline, the position mask, the random-rank formula in two files). Each change alters what the numbers MEAN, and a report written before it still sits on disk reading like a current measurement — the same defect already caught twice in prose (`probe_market_layer_coverage.py`'s hard-coded gridiron narrative, `compare_trajectory_sports.py`'s refuted verdict), both times by reading. `check_artifact_freshness.py` finds them by asking. **`data/ablation_report.json` was 569.5h (23.7 days) behind its producer** — written 2026-07-10, four gate redefinitions ago, with a G4 column computed on a 4,000-row sample against no baseline, and nothing said so. **It also caught one I had missed**: `stage2_report.json` was 0.3h behind `eval_unified.py` because I re-ran it before the last edit to a file it transitively depends on — declared via `also_depends_on`. Method is mtime, and the limit is stated in the docstring rather than glossed: it cannot tell a comment edit from a formula change and will report stale after a docstring fix. **That is the right direction to err — a false "re-run" costs a re-run, a false "fresh" costs a wrong number in a report.** Registration mandatory as everywhere else: an undeclared `data/*.json` is a FAILURE, with `NOT_GENERATED` listing the hand-authored anchors and inputs so the check stays meaningful instead of being switched off. Mutation-verified (drop in an unregistered artifact -> exit 1). Registered in `validate.py`, now 4 checks. **The gate is legitimately RED as of this commit** — `ablation_report.json` is still stale and its re-run is in flight on the GPU alongside the dottie trainer.
- [x] 7.24 **Ablation re-run on the consolidated gates. ONE of five losses earns its keep; the deltas for three others are too small to distinguish from noise this harness never measures.**

        config     G1     G2acc    dMaj  rank  G3sil  G4hit  vs rand
        full       PASS  0.7121 +0.0863  12.8 0.6781   0.96  +0.7888
        no_supcon  PASS  0.6491 +0.0233  19.6 0.1281   0.14  -0.0312 BELOW
        no_coral   PASS  0.7037 +0.0779  12.9 0.6749  0.961  +0.7898
        no_grl     PASS  0.7382 +0.1124  12.9 0.6803  0.961  +0.7898
        no_vicreg  PASS   0.695 +0.0692  12.8 0.6775  0.958  +0.7868
        task_only  PASS  0.7679 +0.1421  18.8 0.1294  0.126  -0.0452 BELOW

    **SupCon is load-bearing and nothing else clearly is.** Dropping it takes G4 from 0.96 to **0.14** — ~~below the 0.1712 random baseline~~ **[CORRECTED in 7.27: that was one seed. The 3-seed mean is 0.204, ABOVE baseline; per-seed 0.113 / 0.171 / 0.328. The collapse is real, the below-chance claim is not.]** — and G3 silhouette from 0.678 to 0.128. `task_only` lands in the same place (G4 0.126, also below random) and additionally has the **worst leakage of any config, +0.1421 over the majority floor**. Cross-sport archetype coherence is SupCon's doing, and without it the fold is worse than chance rather than merely weak — a statement the bare hit-rate could not make until the baseline sat beside it. **CORAL and VICReg move nothing**: dG3 -0.003 / -0.001, dG4 +0.001 / -0.002, dRank +0.1 / +0.0. **GRL moves 2.6pp in the right direction** (removing it raises leakage 0.7121 -> 0.7382), which is larger than the ~1pp recorded in Phase 2 but still small. **BINDING CAVEAT: one seed per config, no repeats, so none of these deltas has an error bar and the harness cannot tell a 0.003 effect from zero.** Three of five losses are being kept on evidence that has not been shown to exceed run-to-run variance. Also visible: the two collapsed configs carry the HIGHEST effective rank (19.6, 18.8 vs 12.8) — direct confirmation of 7.19's point that rank is not a quality signal. Gate now **4 PASS** including `artifact_freshness`, closing the 569h staleness.
- [x] 7.27 **3-seed ablation landed. THREE of five losses are indistinguishable from seed noise — and it walks back my own "below chance" claim from 7.24.** Seeds 7/8/9, 18 trainings.

        config     G1     G2acc    dMaj  rank  G3sil  G4hit  vs rand
        full       PASS  0.6952 +0.0694  13.1 0.6718 0.9617  +0.7905
        no_supcon  PASS  0.6509 +0.0251  19.5 0.1397 0.2040  +0.0328
        no_coral   PASS  0.6911 +0.0653  13.0 0.6759 0.9597  +0.7885
        no_grl     PASS  0.7424 +0.1166  13.0 0.6746 0.9593  +0.7881
        no_vicreg  PASS  0.6753 +0.0495  12.9 0.6764 0.9593  +0.7881
        task_only  PASS  0.7601 +0.1343  19.1 0.1358 0.2003  +0.0291

        drop no_supcon : dG3=-0.532(+-0.010)*  dG4=-0.758(+-0.079)*  dG2sport=-0.044(+-0.020)*
        drop no_coral  : dG3=+0.004(+-0.002)ns dG4=-0.002(+-0.002)ns dG2sport=-0.004(+-0.021)ns
        drop no_grl    : dG3=+0.003(+-0.003)ns dG4=-0.002(+-0.002)ns dG2sport=+0.047(+-0.024)ns
        drop no_vicreg : dG3=+0.005(+-0.003)ns dG4=-0.002(+-0.002)ns dG2sport=-0.020(+-0.019)ns
        drop task_only : dG3=-0.536(+-0.010)*  dG4=-0.761(+-0.081)*  dG2sport=+0.065(+-0.019)*

    **CORRECTION TO 7.24, and it is mine.** I wrote that dropping SupCon "drives cross-sport archetype coherence below chance" on the strength of a single seed reading 0.14. **Per-seed G4 is 0.113 / 0.171 / 0.328, mean 0.204 — ABOVE the 0.1712 baseline.** Two of three runs land below it and one lands well above. The collapse is real and enormous (0.96 -> 0.20); the below-chance claim is not supported and is withdrawn. Exactly the failure the seed-repeat existed to catch, caught on my own claim. **The variance is itself the finding**: `no_supcon` G4 sd is **0.1112** against `full`'s **0.0015** — 74x. Without SupCon the fold is not merely weak, it is *unstable*, landing anywhere between 0.113 and 0.328 on identical data. **CORAL, GRL and VICReg: every delta `ns`.** CORAL and VICReg move 0.002-0.005 against noise floors of the same size — no effect worth the term. **GRL is the honest edge case**: dG2sport +0.047 against a 2x-pooled-sd threshold of **0.048**. It misses by 0.001, and only 2 of 3 seed pairs move in its favour. That is **"not established at 3 seeds"**, not "does nothing" — the right next step is more seeds, not a removal decision. Three of five losses are still in the model on evidence that does not clear a noise floor.
- [ ] 7.28 Resolve GRL with more seeds (10+) — it sits at 0.047 against a 0.048 threshold, the only genuinely undecided loss. Everything else is settled.
- [x] 7.26 **Docs carried the superseded gate numbers. Corrected in place, struck rather than overwritten.** Every gate correction from 7.16-7.24 lived only in code and commit messages; `SPEC.md`, `STAGE2_PLAN.md` and `STAGE2.1_SWEEP_PLAN.md` still stated the old ones as current measurements — the same stale-prose-beside-live-numbers defect fixed twice in `probe_market_layer_coverage.py` and `compare_trajectory_sports.py`. Added a **CORRECTIONS 2026-08-03** section to `SPEC.md` covering all five: G1's `position 1.0 all` (mask-as-index, true 0.784 / 0.999 / 0.879 with majority baselines, and pitch is the one place z is WORSE than e_s), G2's unreachable `<=0.433`, G2's untestable rank, G3's bare `silhouette > 0` and vacuous separation threshold, G4's unstated 0.60 bar (real baseline 0.1712), the withdrawn 3.23x retrieval claim, and Stage 2's `SHIPPABLE=False`. Superseded values are **struck, not overwritten** — a number that changed meaning is more useful than one that quietly changed value. Both Stage 2 planning docs got a SUPERSEDED banner above their targets. **`STAGE2_PLAN.md` §184.4 had already asked "reconsider whether G2<=0.433 is the right target"** — it was not, and the reason is arithmetic rather than modelling; quoted in the correction. **`UNIFIED_ARCHITECTURE.md` §Pillar 3b needed no correction and is flagged as such**: it specifies "clearly below a no-debiasing baseline", a comparison against a CONTROL rather than a constant, which is why `--baseline-sport-acc` was immune to the whole class. Added only a note that "at chance" there means the 0.6258 majority floor, not 1/3.
- [x] 7.29 **The SHIPPED asset carries the unreachable target in its own metadata — and I was wrong about what ships.** Checked `assets/unified.json` against the matrix. Row counts agree (20,719 both; `SPEC.md` §9's "20,721" was the stale one, fixed). Three findings. (a) **`g2_target: 0.433`, `g2_status: "soft_target_not_met"` are baked into the shipped artifact**, where every downstream consumer reads them, against `g2_sport_acc: 0.6851`. Under the corrected target of **0.7258** that status is wrong — it is met, weakly. `export_unified_stage2.py` now computes the target from the class sizes and emits `g2_majority_baseline` and `g2_delta_vs_majority` beside it, with a note to quote the delta rather than the status. (b) **`g1_verdict` carries `pos_drop: 0.0, pos_ok: true` for all three sports** — the mask-as-index bug baked in; a `g1_pos_caveat` field now travels with the asset saying so and giving the true ~0.78 / 0.999 / 0.88. (c) **CORRECTION TO MY OWN CLAIM.** I wrote "nothing has been promoted, `assets/` remains Stage 1 v0.1" in 7.20 and repeated it in 7.26 and in three board reports. **Wrong.** `assets/unified.json` has been **Stage 2.1** (`unified_stage2_best.pt`, best_epoch=58) since **2026-07-30**. `SPEC.md` §155's "Stage 1 v0.1 remains shipped" describes the 2026-07-11 run and was overtaken. What is genuinely unpromoted is the **2026-07-11 Stage 2 checkpoint** — I conflated a checkpoint with the shipped asset. The open operator question is narrower than I stated: Stage 2.1 already ships; the call is only whether the re-scored Stage 2 run supersedes it. **`assets/unified.json` needs a rebuild to pick up any of the metadata fixes** — not done here, because rebuilding the shipped asset is an operator action.
- [ ] 7.9 Pitch expectation signal — needs transfer values (API-Football, blocked on a key; same blocker as 4b.5b). Equities has SEC fundamentals but no analyst-consensus join.

**Three absence claims were made this session without grepping first** — that hoops shipped a trajectory axis (it ships a role-CHANGE axis), that T0/T1 had no counterpart (hoops pedigree joins at 100%), and that no repo joined pedigree (both hoops and gridiron do). Treat "X does not exist anywhere" as a claim requiring a search, the same way a number requires a measurement.
- [x] 7.7g **Invariant checker for the draft-value artifacts** (`check_draft_value_invariants.py`). The table took three wrong versions and every one looked publishable — each was caught by an INTERNAL CONTRADICTION, never by an implausible value, and each time by attention rather than by a check. Five assertions, three of them encoding bugs that actually shipped: **I1** survivor count > its own denominator (25 drafted / 28 survivors, the pool mismatch); **I2** low-survival bucket reporting ~0% never-played (survivor-only denominator, truth was 71.6%); **I3** negative floored EV (unfloored VOR let a wasted 7th-rounder outrank a franchise QB); **I4** drafted counts disagreeing across artifacts that read the same CSV; **I5** a later round worth more than an earlier one. NON-VACUITY: each of the five was reproduced in the artifact and confirmed to fire; restored clean.

**The transferable lesson, since it recurred all session:** face validity passed all three wrong tables. What worked was asking *can these numbers be true TOGETHER* — the same check that caught `skills.json` order-alignment and the `data/` gitignore. Prefer cross-consistency assertions over plausibility review.

- [x] 7.7h **7.7b CLOSED — and the cross-sport finding is OVERTURNED.** Built hoops delivery as fantasy VOR to MATCH gridiron's construct (`build_hoops_vor_draft_value.py`): same composite scoring, floored VOR, 5-year window, `1 - log1p(pick)/log1p(MAX_PICK)` expectation, denominator from `draft_history.json` (7,383 players, not survivor-biased `pedigree.json`). Matched result, 10,000 bootstrap resamples:

        gridiron n=1903 r=+0.3950 CI [+0.3463, +0.4433]
        hoops    n=1508 r=+0.4534 CI [+0.3982, +0.5047]
        gap gridiron - hoops = -0.0584  CI [-0.1296, +0.0156]  DOES NOT exclude zero

    **The +0.1638 "football draft position is more predictive" result does not survive construct matching.** It reverses in direction and loses significance. hoops moved +0.2598 -> +0.4534 purely from replacing `impact` percentile with fantasy VOR — the construct was doing almost all the work. 7.7b was flagged as the binding caveat and it turned out to BE the finding.
